acme_orders:
  organization: acme
  name: Orders Pipeline
  slug: orders-pipeline
  environment: production
  status: active
  default_retention_days: 30
  max_payload_size_bytes: 262144
  default_replay_policy: manual_allowed
  allowed_source_identifiers_text: "orders-worker\nbilling-consumer"

beta_dispatch:
  organization: beta
  name: Dispatch Router
  slug: dispatch-router
  environment: staging
  status: active
  default_retention_days: 14
  max_payload_size_bytes: 131072
  default_replay_policy: manual_allowed
  allowed_source_identifiers_text: "dispatch-service"
